<h1>Listing carts</h1>

<table>
  <tr>
    <th>User</th>
    <th>Product</th>
    <th>Farmersmarket</th>
    <th>Quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @carts.each do |cart| %>
  <tr>
    <td><%= cart.user %></td>
    <td><%= cart.Product_id %></td>
    <td><%= cart.FarmersMarket_id %></td>
    <td><%= cart.quantity %></td>
    <td><%= link_to 'Show', cart %></td>
    <td><%= link_to 'Edit', edit_cart_path(cart) %></td>
    <td><%= link_to 'Destroy', cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cart', new_cart_path %>
